# ai-setup

AI prompt:
```
1. Download all files from https://github.com/ieatcode4breakfast/ai-setup except README.md, ai-setup-gitignore.txt, and tui.json
2. If files already exist in the current project/directory, delete the current files and download the ones in the remote ai-setup repository
3. Add  everything in ai-setup-gitignore.txt to the global gitignore without removing existing global gitignores
4. Ensure that everything that was downloaded from the ai-setup repo is not being tracked in the current project
```
