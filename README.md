# ai-setup

AI prompt:
```
1. Download all files except README.md and ai-setup-gitignore.txt
2. Add all everything in ai-setup-gitignore.txt to the global gitignore without removing existing global gitignores
```