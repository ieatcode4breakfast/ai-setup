# ai-setup

AI prompt:
```
1. Download all files from https://github.com/ieatcode4breakfast/ai-setup except README.md and ai-setup-gitignore.txt
2. If the files already exist in the current project/directory, delete the current files and download the ones in the remote ai-setup repository
3. Append everything in ai-setup-exclude.txt to .git/info/exclude without removing existing excludes
4. Ensure that everything that was downloaded from the ai-setup repo is not being tracked in the current project
```
